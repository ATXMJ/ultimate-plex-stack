# Non-secret configuration values for Ultimate Plex Stack
#
# This file is meant to be used with:
#   docker compose --env-file .env.config up -d
#
# Secrets (passwords, API keys, tokens, claim codes, etc.)
# must live in .env.secrets and are NEVER committed to version control.

# User/Group IDs
PUID=1000
PGID=1000

# Timezone
TZ=America/Denver

# Media Paths
MEDIA_ROOT=C:/plex-server/ultimate-plex-stack/media
DOWNLOAD_ROOT=C:/plex-server/ultimate-plex-stack/downloads
CONFIG_ROOT=C:/plex-server/ultimate-plex-stack/config

# VPN Configuration (provider & behavior only – no credentials here)
VPN_PROVIDER=nordvpn
VPN_ENABLED=true
VPN_DNS=103.86.96.100,103.86.99.100
VPN_PROTOCOL=wireguard
VPN_PORT=443
VPN_KILL_SWITCH=true

# Domain (for reverse proxy)
DOMAIN=cooperstation.stream
ACME_EMAIL=txaggiemichael@gmail.com


