#
# NOTE: This file is intentionally left without real values.
# Docker Compose will NO LONGER read configuration from `.env` directly.
#
# Instead, configuration is split into two explicit files:
#
#   - .env.config  → non‑secret configuration (paths, timezone, domain, etc.)
#   - .env.secrets → sensitive values (passwords, API keys, claim tokens, etc.)
#
# Usage examples:
#   docker compose --env-file .env.config up -d
#   docker compose --env-file .env.config run --rm buildarr apply
#
# See `.env.config` and `.env.secrets` for actual values.
